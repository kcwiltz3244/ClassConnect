ClassConnect v2.8.4 — Visible Return to Home Fix

Upload these three files to GitHub and replace the existing files:
- index.html
- styles.css
- function-guide.html

This fix places a clearly visible Return to Home button directly inside every
feature page and uses inline formatting so the button does not depend on an old
cached stylesheet.

After committing:
1. Wait for the Cloudflare deployment to finish.
2. Open ClassConnect in the browser.
3. Refresh the page.
4. On iPhone, close and reopen the Home Screen app if it was already open.

Suggested commit:
Fix visible Return to Home buttons v2.8.4
